{
  "name": "ui",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo run check-types",
    "consumer": "pnpm --filter=consumer-vite",
    "ui-docs": "pnpm --filter=ui-docs",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "ui": "pnpm --filter=@kaotypr/ui"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "turbo": "^2.7.4",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
